#include <stdio.h>
int main()
{
    int arr[7], i;
    printf("Enter the number of element:");
    scanf("%d\n", arr[i]);
    int max = arr[0];
    for (i = 1; i <= 6; i++)
    {
        if ( max < arr[0])
        {
            int max=arr[i];
        }
     printf("%d\n", arr[i]);
}
return 0;
}