#include <stdio.h>
int main()
{
    int n, r, i;
    printf("Enter n and r:");
    scanf("%d %d", &n, &r);
    int nfact = 1; // n!
    int rfact = 1; // r!
    int nfact = 1; // nr!
    for (i = 2; i <= n; i++)
    {
        nfact = nfact * i;
    }
    for ()
}